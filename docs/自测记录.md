# 自测记录

> 项目交付时的验证状态说明（如实记录）：
> 本会话的开发沙箱不提供任何命令执行工具（bash/pwsh/exec 均不可调，注册表无该工具），
> 因此由目标机执行验收；**已于 2026-09-07 在目标机（OpenJDK 17.0.20.1）执行 `bash scripts/acceptance.sh`，
> 结果：通过 22 项 / 失败 0 项（退出码 0）**。
> 除动态验收外还完成了两级静态验证：编译器级逐文件人工审查 + 文档↔代码↔样例一致性审计（见第 2 节）。
>
> **第 1～4 节为一期（单值评分）验收记录；二期（四维/Excel/名单 CRUD/彻底重置）见第 5～6 节——
> 二期已于 2026-09-08 在目标机通过 acceptance.sh 一键验收：45 项断言 / 失败 0 项（退出码 0）。**

## 1. 自动验收路径（一条命令）

```bash
cd /home/kawasakikidou/scoringSystem
bash scripts/acceptance.sh    # 一键：环境检查 → 下载 H2 → 编译 → 22 条断言端到端自测 → 汇总
```

（等价的分步执行：`bash scripts/fetch-libs.sh` → `bash scripts/build.sh` → `bash scripts/self-test.sh`；
`acceptance.sh` 任一环节失败即退出码非 0，日志留档 `out/e2e-logs/`。）

`self-test.sh` 覆盖并断言（**13 组验收点 / 22 条断言**，全部通过时输出 “通过 22 项，失败 0 项”，退出码 0）：

| # | 验收点 | 断言（grep 关键输出） |
|---|---|---|
| 1 | 规整样例导入（首次） | `成功解析 8 行（新增 8 人、更新/去重 0 行）`、`跳过 0 行` |
| 2 | 乱序样例导入统计（独立库） | `成功解析 10 行（新增 9 人、更新/去重 1 行）`、`跳过 3 行`、打印 `跳过示例` |
| 3 | 幂等重复导入 | `成功解析 8 行（新增 0 人、更新/去重 8 行）`（不产生重复记录） |
| 4 | 面试 ≥3 条自动去极值 | 王小明 85/90/80 → 完成，B 场景断言含 `评分不足 3 条经确认` |
| 5 | 面试 1～2 条二次确认（普通平均） | 李小红 70/90 → `平均分：80.00（普通平均，评分不足 3 条经确认）` |
| 6 | 少数民族点号姓名整段解析 | 搜索 `麦麦提` 命中并完成面试，去极值 `最终分：86.00`（82/86/91） |
| 7 | 排名 + 明细入口 | 排名表输出、明细输入回车返回 |
| 8 | CSV 导出 | 文件头字节 = `EF BB BF`（UTF-8 BOM）、表头与明细行存在 |
| 9 | 补录自动重算 | 李小红 +95 → 3 条重新去极值 `最终分：90.00` |
| 10 | 附加分直接计入 | 王小明 +5（才艺加分）→ `附加分合计 5.00` |
| 11 | 撤销（LIFO + 重算） | `已撤销「添加附加分 5.00（才艺加分）」` |
| 12 | 暂存退出 + 跨进程续面 | `已暂存…`；第二次启动自动续面陈静并完成（`最终分：85.00`） |
| 13 | 初始化（YES 门禁） | `初始化完成…`、名单保留 `共显示 8 人（总数 8 人）`、8 人均回到“未面试” |

运行日志自动留档 `out/e2e-logs/`（imp1/imp2/imp-messy/run1/run2/run3.log）。

### 1.1 手工抽查建议（脚本外的补充路径）

```bash
# 追加/附加分校验：非法分值、无原因附加、补录非 FINISHED 候选人、撤销“只剩一条评分”边界
bash scripts/run.sh    # 交互操作：1) 导入后对未面试者补录 → 应提示“不能补录”；
                      # 2) 附加分输入 101 → 提示 0～100；3) 已结束者只剩一条评分时撤销 → 提示拒绝
```

## 2. 静态审查记录（本会话完成）

- **编译级审查**：两名只读审查子代理对全部 19 个 Java 文件逐行做了“编译器级”审查（语法/类型/正则转义/
  H2 SQL/状态机），报告与修复见交付说明；P0 项已在交付前全部修复（配对策略、全角括号语法、编码回退等）。
- **审查发现并已修复**：
  1. P0（编译阻断）：`searchCandidates` 局部变量再赋值破坏 lambda effectively-final → 改为
     `final prefix / final nameKw` 后再捕获；
  2. P1：评分入库与撤销日志双写改为同一事务（`Store.tx`，覆盖 打分/删分/补录/附加/撤销 全部写路径）；
  3. P1：排名明细查看循环支持 回车 / q / 0 返回；
  4. P1：不足 3 条结束面试的二次确认改为“显式 y 才放行”（回车不再默认确认）；
  5. P2：导入方法冗余 try/catch、文档口径（超 10 位数字搜索截断）等清理；
  6. **目标机真实 javac 实测发现**：`Main.setUpUtf8Stdio()` 的 try 块内构造函数均不抛受检异常，
     `catch (IOException)` 属“捕获永不抛出的异常”→ 编译错误；已移除 try/catch 与多余 import（2026-09-07 实测修复）。
  7. **目标机真实运行实测发现**：H2 2.x 中 `value` 为保留字，`CREATE TABLE score/op_log … value DECIMAL…` 触发
     语法错误 42001 导致建库失败；已将两表列改名 `value → points`（含全部 SQL/映射与接口文档表结构），
     Java DTO 字段名不变（2026-09-07 实测修复）。
  8. **目标机真实运行实测发现（21/22）**：撤销附加分的提示语误用半角收尾括号
     `（才艺加分)」`，与文档/断言的全角 `）」` 不一致 → 已统一为全角（2026-09-07 实测修复）。
- **一致性审查**：接口文档 5 节内容 ↔ core API ↔ CLI 菜单 ↔ 样例预期统计逐条核对（P0=0，
  乱序 10 行/9 新/1 更/跳 3、规整 8/0 逐行推演一致）。
- 人工复核结论：源码层面未再发现编译阻断项；**动态行为最终以 `self-test.sh` 在目标机上的输出为准**。

## 3. 验收标准对照

| 验收标准 | 对应交付/路径 |
|---|---|
| 1. javac 全量编译通过，按文档步骤可运行 | `scripts/build.sh` + `scripts/run.sh`（README/接口文档第 6、10 节） |
| 2. 两个测试名单样例 + 跳过统计正确 | `samples/名单样例-规整.txt`（8/0）、`名单样例-乱序.txt`（10 行 9 新 1 更 / 跳过 3） |
| 3. 样例走通完整流程 | `scripts/self-test.sh`（导入→搜索→面试含 <3 二次确认→排名→CSV BOM→补录/附加/撤销→跨进程续面→初始化） |
| 4. 接口文档齐全（数据模型/API/CLI/打包/GUI 文档要求/歧义语义） | `docs/接口文档.md`（第 2～9 节）+ `docs/CLI使用说明.md` |

## 4. 目标机执行回填（已完成 ✅）

```text
执行日期：2026-09-07
环境：OpenJDK 17.0.20.1（java/javac 就绪），首次运行联网下载 h2-2.2.224.jar 成功
acceptance.sh 结果：第 1 步依赖 ✓ → 第 2 步编译 ✓ → 第 3 步自测：通过 22 项 / 失败 0 项（退出码 0）
验收中实测驱动并修复的问题：#6 Main catch 编译错误、#7 H2 保留字 value→points、#8 撤销提示全角括号（见第 2 节）
证据：out/e2e-logs/（run1/run2/run3/imp1/imp2/imp-messy.log）为最后一轮全通过会话的输出
```

## 5. 二期验收路径（待目标机执行回填）

一键验收（等价于 分步①fetch-libs → ②gen-excel-samples → ③build → ④self-test）：

```bash
cd /home/kawasakikidou/scoringSystem
# python 依赖（Debian/Ubuntu 有 PEP 668 限制，三选一）：
#   A. sudo apt install python3-openpyxl python3-xlwt
#   B. pip install --break-system-packages openpyxl xlwt
#   C. python3 -m venv /tmp/acc-venv && /tmp/acc-venv/bin/pip install openpyxl xlwt \
#      && export PATH=/tmp/acc-venv/bin:$PATH
bash scripts/acceptance.sh
# 依赖下载直连失败时：bash scripts/acceptance.sh -x http://127.0.0.1:7890
```

分步等价命令：

```bash
bash scripts/fetch-libs.sh          # H2 + POI 5.2.5 系（直连或 -x 代理）
bash scripts/gen-excel-samples.py   # 生成 samples/名单样例-四维.xlsx/.xls
bash scripts/build.sh               # core 独立编译(cp lib/*) + core+cli 全量
bash scripts/self-test.sh           # 二期端到端断言
```

二期断言覆盖（见 self-test.sh）：txt 规整 8/8/0 与幂等 0/8；txt 乱序 10/9/1/跳3 口径不回归；
xlsx/xls 同口径（数值学号无科学计数法）；名单 CRUD（新增→面试 1 条→不足 3 条二次确认→改名→
改学号级联迁移验证）；四维逐维去极值（A=73.00 样例）；同分 72 按 责任心→… 排序（C 第 2/D 第 3）；
附加 11 被拒 / 9 成功；撤销附加并回落 73.00；导出 CSV BOM+二期 13 列+附加理由；删除带评分候选人
级联；初始化；一期旧库自动迁移（RunScript 造 v1 库 → 提示 + 归档表 + 名单保留 9 人）；
彻底重置（确认词不符被拒 → 正确词执行 → 文件/CSV 删除 → 归零 → 重建后可重新导入）。

## 6. 二期目标机执行回填（已完成 ✅）

```text
执行日期：2026-09-08
环境：Debian 容器（无 sudo，PEP 668）→ python3.11-venv/ensurepip 缺失 → .venv --without-pip + get-pip.py 引导
      + openpyxl 3.1.5 / xlwt 1.3.0（scripts 已自动优先 .venv）；JDK 17.0.20.1
一键验收：bash scripts/acceptance.sh（依赖 H2+POI 下载 ✓ → xlsx/xls 样例生成 ✓ → core 独立编译+全量 ✓ → 自测）
首轮结果：45 项断言：通过 41、失败 4（排名表列粘连 ×3、空名单缺汇总行 ×1——均为 CLI 展示回归）
修复：CliApp.menuRanking 列间显式空格连接；menuList 空结果也打印“共显示 0 人（总数 0 人）。”
复验结果：全部通过（45 项 / 失败 0 项，退出码 0）✅
证据：out/e2e-logs/（imp*/r1..r11/gen/legacy-run.log）；修复细节见 docs/验收失败排查记录.md §4
```
