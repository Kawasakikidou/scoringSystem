# 编译产物
out/
# 数据库文件（H2 单文件库 + 锁/日志）
data/*.mv.db
data/*.trace.db
data/*.lock.db
# 导出与备份
data/*.csv
*.csv
# 临时文件
*.tmp
# JavaFX SDK（体积大，由 scripts/fetch-gui-libs.sh(.bat) 按需下载）
lib/openjfx-17/
# 打包用临时输入目录（jpackage 输入 staging，不入库；dist/ 为发布产物，随 Release 一起入库）
dist-pkg/
